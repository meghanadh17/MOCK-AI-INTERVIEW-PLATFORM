<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:color="@color/text_prim" android:state_focused="true" />
    <!-- Hovered state -->
    <item android:color="@color/border_accent" android:state_hovered="true" />
    <!-- Default state -->
    <item android:color="@color/border_strong" />
</selector>
